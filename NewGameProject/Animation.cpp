#include "Animation.hpp"

Animation::Animation(int width, int height, int pattern, Texture texture)
{
    this->width = width;
    this->height = height;
    this->pattern = pattern;
    this->texture = texture;
}
